# タスクリスト (v7.2.23)

- [x] `rinkan_umis_pro_v7.2.23.py` の作成（v7.2.22ベース）
- [x] `VERSION` を `7.2.23` に更新
- [x] `__init__` に `self.lbl_rename_preview` を追加
- [x] `build_ui` の `dest_row` でボタンを左側に移動
- [x] `build_ui` のプログレスエリアに `self.lbl_rename_preview` を配置
- [x] `update_preview` でリネームサンプルの表示ロジックを実装
- [x] 動作確認
