# UMIS Pro v7.2.18 欠落機能の強制実装（厳格化版）実装計画

## 概要
ユーザーから提供された具体的なコードスニペットを、一切の改変や省略なく `rinkan_umis_pro_v7.2.18.py` に組み込みます。Flet特有のイベント処理やUIの整合性を、ユーザーの期待通りに確実に実現します。

1. **変数の確実な定義**: `__init__` にて Shift 状態ラベルや開始インデックス、最終取り込みドライブ変数を定義します。
2. **ツールバーの刷新**: 指定されたボタン配置とスタイルでツールバーを再構築します。
3. **Shiftキー監視の厳格化**: キーボードイベントとファイルクリック時の連動ロジックを、提供されたコードで完全に上書きします。
4. **安全ロジックの修正**: 初期化ボタンの有効化条件を、ユーザー指定の論理式に書き換えます。

## 変更内容

### UMIS App 本体

#### [MODIFY] [rinkan_umis_pro_v7.2.18.py](file:///Users/kiroku_keizo/開発/UMIS%20(Universal%20Media%20Ingest%20System)/安定版/rinkan_umis_pro_v7.2.18.py) [NEW]

- **VERSION更新**: `VERSION = "7.2.18"` に更新。
- **__init__**: 指示された変数を追加。
- **build_ui**: ツールバーとキーボードイベント（`on_keyboard`）を指定コードで上書き。
- **on_file_click**: 範囲選択ロジックを指定コードで上書き。
- **select_unassigned**: 指定コードで新規メソッドを追加。
- **check_format_button_state**: 指定コードで論理を上書き。

## 検証計画
- アプリケーションが起動することを確認。
- ツールバーの「未割当を選択」ボタンの色が `COLOR_ACCENT` になっていることを確認。
- Shiftキーを押した際、ラベルが太字で "Shift: ON" と表示されることを確認。
- 範囲選択実行後、ラベルが即座に "Shift: OFF" に戻ることを確認。
- 初期化ボタンの有効化条件（緊急フォーマット or 取り込み完了ドライブ一致）が意図通りであることを確認。
